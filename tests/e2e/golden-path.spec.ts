import { test } from "@playwright/test";

test.skip("golden path requires completed browser fixtures, Postgres, and MinIO", async () => {
  // This is intentionally skipped until the prototype browser flow is fully scripted.
});
